---
# ── Identity ────────────────────────────────────────────────
id: unit-2-events-putt-and-play-2026-09-19
unit: "Unit 2 · Events"
title: "Putt and Play"
accent: blue
planPath: "workshops/Computer Science & AI 8+/Unit 2 - Events - Putt and Play.md"

# ── Luma ────────────────────────────────────────────────────
# Fill both in AFTER creating the event, from
# Manage → More → Embed Registration Button.
lumaEventId: "REPLACE-ME"
lumaUrl: "REPLACE-ME"

# ── Scheduling ──────────────────────────────────────────────
# startsAt must be a full ISO timestamp with offset.
#   -04:00 = EDT (Mar–Nov)   ·   -05:00 = EST (Nov–Mar)
# durationHours: 2 = core workshop, 4 = core + lunch + extension.
startsAt: "2026-09-19T10:00:00-04:00"
durationHours: 2
location: "ByteBear Studio — Toronto, ON"
isOnline: false

# ── Audience & capacity ─────────────────────────────────────
ages: "Ages 8–11"
capacity: 12

# ── Publishing ──────────────────────────────────────────────
# draft → not on the site (safe while Luma fields are placeholders)
# scheduled → live and bookable
# cancelled → shown as cancelled, tells Google the event is off
status: draft

blurb: "Programs that react. Teams use events and colour messages to drive a bee, then build music machines that respond to key presses and colour."
---

## What we'll build

This session is about programs that *wait for something to happen*. Teams start with a
bee that responds to colour messages, then build music machines triggered by key
presses and colour changes — debugging along the way when a trigger fires at the wrong
moment.

## What your child will learn

- What an event is, and how it differs from a plain sequence
- Sending messages between separate scripts so parts of a build stay in sync
- Finding and fixing a bug by isolating which trigger misfired

## How the session runs

Students work in pods of four with one LEGO kit and one device per pod, rotating
through Designer, Builder, Programmer and Tester-documenter so everyone codes and
everyone builds. The session always ends with a working project and a short demo.

The 4-hour version adds a lunch break and an interactive mini-golf challenge, where
teams design a hole that reacts to the ball.

## What's included

Every pod gets a LEGO Education Computer Science & AI kit and a tablet. Unit 1 is
helpful but not required — students who are comfortable with basic sequences can
start here.

## Fees

Your seat is confirmed by email, and we invoice after the session.
